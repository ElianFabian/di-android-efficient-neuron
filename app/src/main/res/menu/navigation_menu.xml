<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/navProfile"
                android:title="@string/navigation_menu__navProfile_title"
                android:icon="@drawable/ic_account" />

        <item
                android:id="@+id/navTips"
                android:title="@string/navigation_menu__navTips_title"
                android:icon="@drawable/ic_tips" />

        <item
                android:id="@+id/navStatistics"
                android:title="@string/navigation_menu__navStatistics_title"
                android:icon="@drawable/ic_statistics" />

        <item
                android:id="@+id/navSettings"
                android:title="@string/navigation_menu__navSettings_title"
                android:icon="@drawable/ic_settings" />
    </group>

</menu>